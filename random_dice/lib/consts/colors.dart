import 'package:flutter/material.dart';

const backgroudColor = Color(0xFF0E0E0E);
const primaryColor = Colors.white;
final secondaryColor = Colors.grey[600]; // 600이라는 키값을 입력하면 런타임에 색상이 계산되므로 final 사용, const 안됨.